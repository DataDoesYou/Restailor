# Frontend

## Troubleshooting: VS Code is stuck analyzing
- Run `npm run clean:ts`
- Command Palette → "TypeScript: Select TypeScript Version" → "Use Workspace Version"
- Command Palette → "TypeScript: Restart TS server"
- If still spinning: start with `code --disable-extensions` and re-enable in batches
